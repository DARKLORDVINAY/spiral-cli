# Spiral Chess CLI

This repository now includes a browser-based chess app with a clean UI and a hard-difficulty bot (minimax with alpha-beta pruning).

## Webpage link

Open the app directly:

- `./index.html`

Or serve locally:

```bash
python3 -m http.server 8000
```

Then visit:

- http://localhost:8000/index.html
